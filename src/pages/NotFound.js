const NotFound = () => {
  return (
    <div
      style={{
        display: "flex",
        height: "100vh",
        justifyContent: "center",
        alignItems: "center",
        fontSize: "40px",
        fontWeight: "bold",
        textAlign: "center",
      }}
    >
      <h1>404 NOT FOUND</h1>
    </div>
  );
};

export default NotFound;
